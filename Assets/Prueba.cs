using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Prueba : MonoBehaviour
{
    // Start is called before the first frame update
    /*void Start()
    {

        SAI.SDK.Login.SignUp("campo@s.com","pedro","+541168983444", "cs", "p");
    }

    private void OnEnable()
    {
        LoginSystem.RegistrationSuccessTrigger += loged;
        LoginSystem.RegistrationFailureTrigger += func;
        LoginSystem.LoginSuccessTrigger += newFunc;
        LoginSystem.LoginFailureTrigger += newFunc1;
        LoginSystem.LogoutSuccessTrigger += function;
    }

    private void OnApplicationQuit()
    {
        SAI.SDK.Login.SignOut();
    }
    private void function()
    {
        print("logout Succes");
    }

    private void newFunc1()
    {
        print("login Failed");
    }

    private void newFunc()
    {
        print("login Succes");
    }

    private void OnDisable()
    {
        LoginSystem.RegistrationSuccessTrigger -= loged;
        LoginSystem.RegistrationFailureTrigger -= func;
        LoginSystem.LoginSuccessTrigger -= newFunc;
        LoginSystem.LogoutSuccessTrigger -= function;
        LoginSystem.LoginFailureTrigger -= newFunc1;
        
    }

    private void func()
    {
        print("Failed");
        SAI.SDK.Login.SignIn("spaceai@spaceai.com","spaceai");//Session Key
    }

    private void loged()
    {
        print("Succes");//newpassword
    }

    // Update is called once per frame
    void Update()
    {
        
    }*/
}
