using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.SceneManagement;

public class OpenMenu : MonoBehaviour
{    

    /*public string pauseMenuSceneName = "menu"; // Assign the name of your pause menu scene in the Inspector

    public void TogglePauseMenu()
    {
        // Alternar entre mostrar y ocultar el menú de pausa
        pauseMenuUI.SetActive(!pauseMenuUI.activeSelf);

        // Pausar o reanudar el tiempo en el juego
        Time.timeScale = (pauseMenuUI.activeSelf) ? 0f : 1f;
    }

    public void LoadPauseMenu()
    {
        SceneManager.LoadScene(pauseMenuSceneName, LoadSceneMode.Additive);
    }

    public void UnloadPauseMenu()
    {
        SceneManager.UnloadSceneAsync(pauseMenuSceneName);
    }*/
}
